---
title: "Fixing bugs by writing tests (write and run test)"
date: 2025-03-16
header:
    teaser: /assets/images/tests-bug.png
---

**Second post in series.**

<p align="center">
  <img src="/assets/images/tests-testthat.png" width="15%" />
</p>

## 2. Write and run a test that should fail

Now that I have found the bug, I will write a unit test that fails when that bug is found, using the `testthat` R package.

### The Plan

The bug arises from the file names in the SuSiEx output. Sometimes Nextflow passes ancestry labels (e.g., SAS, EUR, AFR) in different orders when running SuSiEx. My package assumes that all output files have the same ancestry order. If they don’t, the function isn’t meant to run, it should error out to alert the user that the ancestries in the input files are not ordered correctly.

To catch this issue, I created test data by copying actual SuSiEx output files that have file names with different orders of ancestry labels, and then clearing their contents (since only the file names matter). The test inspects the file names to ensure the ancestry labels appear in a consistent order across all files. If they don't, it triggers an error, letting the user know their input files are wrong. The test will expect this error.

### Write test: [test-format_results.R](https://github.com/AmeliaES/susiexR/blob/11-incorrect-ancestries-seem-to-be-plotted-in-plotancestrycausal/tests/testthat/test-format_results.R)

```r
test_that("ancestry labels in file names are in the same order for all files", {
  
  # Get the path to where the test data is stored
  source_path <- system.file("extdata", "ancestry-labels-not-ordered", package = "susiexR")
  
  # Get the names of files in the test data directory
  file_names <- list.files(source_path, full.names = TRUE, pattern = "\\.summary$|\\.snp$|\\.cs$")
  
  # Define ancestries used in the test data
  ancestries <- c("SAS", "EUR", "AFR")
  
  # All file names should contain the ancestry labels in one order
  # the order may differ to the order in the vector above (given as an argument to format_results function)
  
  # Get all possible orders of ancestries (permutations)
  permutations <- arrangements::permutations(ancestries)
  
  # Collapse the permutations into strings, separated by any common separator (hyphen, underscore or period)
  permutations_strings <- lapply(1:nrow(permutations), function(i) {
    paste(permutations[i, ], collapse = "[-_.]")  # Match hyphens, underscores, or periods
  })
  
  # Check for matches: We want to ensure that only one order is found across all files
  ancestry_string_in_all_file_names <- sapply(permutations_strings, function(perm) {
    # Loop over each ancestry order and check if it is found in all file names
    all(sapply(file_names, function(file) {
      grepl(perm, file)
    }))
  })
  
  # Check if exactly one valid ancestry order is found across all files
  if (sum(ancestry_string_in_all_file_names) != 1) {
    expect_error(format_results(source_path, ancestries = ancestries),
                 "Error: The file names do not have the same order of ancestry labels or the ancestries are not immediately separated by hyphens, underscores, or periods.")
  }
})
```

### Run test:

This test failed as expected.

### Explanation of test

1. Use `list.files` to gather the names of all relevant files in the `ancestry-labels-not-ordered` folder. This is currently in the `format_results()` function.
2. Generate all possible orders of the ancestry labels (eg. SAS, EUR, AFR) to check file names against.
3. For each permutation, check whether it exists in the file names using grepl.
4. Check there is exactly one valid ancestry order across all files. If there are multiple orders or none at all, then an error is expected.

### Improvements to make

- This test is quite long. Refactor code, create smaller, helper functions. Each function should be appropriately named and do one thing. And unit tests each test these small functions.
- Creating a function that extracts ancestry strings from the file would be a good start.


## Next steps

1. Add this logic to the function `format_results()`. 
2. Re-run test, it should now pass.
3. Refactor code.










